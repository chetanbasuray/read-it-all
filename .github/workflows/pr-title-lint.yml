name: PR Lint

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

jobs:
  lint-pr-title:
    runs-on: ubuntu-latest
    steps:
      - uses: amannn/action-semantic-pull-request@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            feat
            fix
            docs
            chore
            refactor
            test
            build
            ci
            perf
            style
            revert

  lint-branch-name:
    runs-on: ubuntu-latest
    steps:
      - name: Check branch name is feat/*, fix/*, or a Dependabot branch
        run: |
          if [[ ! "${{ github.head_ref }}" =~ ^(feat|fix)/.+ ]] && [[ ! "${{ github.head_ref }}" =~ ^dependabot/ ]]; then
            echo "Branch '${{ github.head_ref }}' must start with feat/, fix/, or dependabot/"
            exit 1
          fi
